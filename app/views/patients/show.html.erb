
<% @patient.appointments.each do |appointment|%>
<h1> <%= appointment.appointment_datetime.strftime("%B %d, %Y at %H:%M") %> </h1>
<h1> <%= link_to appointment.doctor.name, doctor_path(appointment.doctor) %> </h1>
<% end %>
