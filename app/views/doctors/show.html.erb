<% @doctor.appointments.each do |x| %>
<p><%= x.appointment_datetime.to_datetime.strftime('%B %d, %Y at %H:%M')%></p>
<% end %>

<% @doctor.patients.each do |patient| %>
    <%= link_to patient.name, patient_path(patient) %>
<% end %>